import { ensureJsonMap, ensureString } from "@salesforce/ts-types";
import expect from "chai";

describe("sfcraft:allure:apex:report", () => {
  it("calls force:apex:test:report to get report", () => {});
});
